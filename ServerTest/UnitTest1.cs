using Microsoft.VisualStudio.TestTools.UnitTesting;
using Moq;
using Sir;

namespace ServerTest
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {

          



            /*    Mock<Server> mock = new Mock<Server>();
                mock.Setup(x => x.txtStatus_TextChanged()).Returns(true);

                Server server = new Server();
                bool isInserted = Server.txtStatus_TextChanged(mock.Object);
                Assert.AreEqual(isInserted, true); */
        }
    }
}
